# CodeRabbit skips any pull request over 100 files. The iOS and Android
# runners are generated platform files nobody hand-edits, so they are excluded
# and the Dart, CI and docs are what get reviewed.
reviews:
  path_filters:
    - "!app/ios/**"
    - "!app/android/**"
